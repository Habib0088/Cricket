import React, { use } from 'react';


const Playes = ({jsonData}) => {
    let data=use(jsonData)
    console.log(data);
    

   
    return (
        <div>
            
        </div>
    );
};

export default Playes;